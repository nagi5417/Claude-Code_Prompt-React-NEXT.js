---
name: plan
description: "調査結果に基づく実装計画を策定する。ユーザーが「計画を立てて」「実装計画」「タスク分解して」「見積もりして」と言ったときに使用する。実装やコーディングには使わない。"
argument-hint: <task-description>
allowed-tools: Read, Glob, Grep
model: sonnet
context: fork
---

# 実装計画: $ARGUMENTS

## ワークフロー

1. 関連する調査ドキュメント（`docs/investigate/`）があれば読み込む
2. 実装スコープを定義する（何を含み、何を含まないか）
3. タスクを分解し、依存関係を整理する
4. 各タスクの実装順序を決定する
5. リスクと対策を明記する

## 出力

以下を `docs/plan/YYYYMMDD_HHMM_<slug>.md` に記録する:

### 実装計画

- 目的と背景
- スコープ（In Scope / Out of Scope）
- 変更対象ファイル一覧

### タスク分解

| # | タスク | 対象ファイル | 依存 | 見積 |
|---|--------|------------|------|------|
| 1 | ...    | ...        | -    | ...  |

### 技術的決定事項

- 選択したアプローチと理由
- 代替案と却下理由

### リスクと対策

- 既知のリスクと緩和策

### 検証計画

- テスト方針
- 動作確認手順

slug は $ARGUMENTS を lowercase + スペースを `-` に変換して生成する。

## 完了条件

- 計画ドキュメントが `docs/plan/` に保存されている
- タスクが分解され、依存関係と見積もりが記載されている
- リスクと検証計画が明記されている